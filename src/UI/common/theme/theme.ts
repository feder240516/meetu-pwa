import { createTheme } from "@mui/material/styles";

const colorTheme = createTheme({
  palette: {
    primary: {
      main: '#eb3aa7',
    },
    // secondary: {
    //   main: '#52BBC4',
    // },
  },
});

export default colorTheme;